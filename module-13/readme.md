
# Tools and Terminal->git,vs code.

# Node.js mmodules.-> use a funtion index.js to app.js a নেয়া।

# Request and response model.-> express install  করে সেইটাকে server হিসেবে ব্যাবহার করে  or hht server  use করে localhost:3000 এ host kora .(also node_module এর    dependancy list থাকে), যেটাকে .gitignore এর মাধ্যমে github এ push দেয়া থেকে থামানো জায়, পরে লাগলে (inp i) লিখার মাধ্যমে আনা জয়ায়। 

# package.json->এখানে সকল package listed  থাকে , like(day.js , experess.js), script এর নিচে "start": "node app.js" লিখার মধ্যমে আমরা fix করে দিতে পারি কোন্টায় শুরু হবে।

# Understanding HTTP client.

# URL module.

